import { makeStyles } from "@material-ui/core/styles";

export const FeatureStyles = makeStyles({
  media: {
    height: 345,
  },
  root: {
    width: 200,
    display: "flex",
    alignItems: "center",
  },
});
